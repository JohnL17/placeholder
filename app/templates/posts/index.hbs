<h3>All posts</h3>

{{#each model as |post|}}
  {{#link-to 'posts.show' post.id class='uk-link-reset'}}
    {{#uk-card color='default' hover=true class='uk-margin-bottom' as |card|}}
      {{#card.header}}
        {{#card.title}}{{post.title}}{{/card.title}}
      {{/card.header}}
      {{#card.body}}{{post.body}}{{/card.body}}
    {{/uk-card}}
  {{/link-to}}
{{/each}}

