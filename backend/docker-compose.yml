version: '3'
services:
  db:
    image: postgres
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=placeholder
      - POSTGRES_PASSWORD=placeholder

  admin:
    image: fenglc/pgadmin4
    links:
      - db
    ports:
      - '3001:5050'
    environment:
      - DEFAULT_USER=api
      - DEFAULT_PASSWORD=123qwe
