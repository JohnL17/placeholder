<form>
    <fieldset class="uk-fieldset">
        <legend class="uk-legend">Create a gallery</legend>
        <div class="uk-margin">
            {{input class="uk-input" value=newTitle placeholder="Title"}}
        </div>
        <button class="uk-button uk-button-primary" {{action 'createGallery'}}>Create</button>
    </fieldset>
</form> 
