<form>
    <fieldset class="uk-fieldset">
        <legend class="uk-legend">Edit the gallery</legend>
        <div class="uk-margin">
            {{input class="uk-input" value=model.title}}
        </div>
        <button class="uk-button uk-button-primary" {{action 'editGallery'}}>Edit</button>
    </fieldset>
</form> 
